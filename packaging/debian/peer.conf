# PiAoIP Raspberry Pi 4 network configuration.
# Empty interface selects the only active wired IPv4 interface; Wi-Fi is excluded.
PIAOIP_INTERFACE=
# Set the Windows Ethernet IPv4 with piaoip-configure before starting audio.
# No computer address is built into the package or libraries.
PIAOIP_PEER=configure-me
